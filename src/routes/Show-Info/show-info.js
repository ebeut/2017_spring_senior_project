import React, { Component, PropTypes } from 'react';

export default class ShowInfoPage extends Component {
  render () {
    return (
      <div id="show-info-page">
        this is the show info page
      </div>
    );
  }
}
